services:
  # Backend API
  - type: web
    name: queztl-core-backend
    runtime: python
    region: oregon
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: queztl-db
          property: connectionString
      - key: REDIS_URL
        fromDatabase:
          name: queztl-redis
          property: connectionString

databases:
  # PostgreSQL Database
  - name: queztl-db
    databaseName: queztl_core
    plan: free
    region: oregon

  # Redis Cache
  - name: queztl-redis
    plan: free
    region: oregon
